version: "3.9"

services:
  backend:
    image: furkanvka/club-backend:latest
    ports:
    - "8080:8080"
    restart: always

  frontend:
    image: furkanvka/club-frontend:latest
    ports:
    - "3000:3000"
    restart: always
